language: node_js
node_js:
- "8"

script: echo "I really should add tests or something here."

jobs:
  include:
    - script: npm audit
    - stage: npm release
      script: npm version patch
      deploy:
        provider: npm
        api_key: $NPM_API_KEY
        skip_cleanup: true
        on:
          branch: master

